<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- BLE 비컨 (PRD §9: Android 12 권한분리 — 스캔은 위치 목적 아님) -->
    <!-- API 30 이하 레거시: BLE 스캔에 위치 권한 필요 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- ACCESS_FINE_LOCATION: maxSdkVersion 제한 금지 — dchs_flutter_beacon 0.6.9의
         checkLocationServicesPermission()이 API31+에서도 BLUETOOTH_SCAN과 함께
         이 권한의 승인 여부를 무조건 확인한다(neverForLocation 플래그를 실제로는 참조하지 않음).
         maxSdkVersion=30으로 제한했더니 API31+ 기기에서 이 권한이 선언 자체가 안 돼
         승인이 영원히 불가능해지고 비컨 스캔이 "항상 불가"로 고정되는 회귀가 있었다(2026-07-06).
         좌표(GPS lat/lng)는 이 권한이 있어도 코드에서 전혀 읽지 않는다 — PI-3 회귀 테스트가 보증. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- BLUETOOTH_SCAN: neverForLocation 플래그 금지 — 이 플래그가 있으면 Android OS가
         스캔 결과에서 iBeacon류(위치 추론 가능) 광고를 걸러내 앱에 전달하지 않는다.
         태블릿이 정상 광고 중인데도 학생 폰이 영원히 감지 실패하는 회귀의 원인(2026-07-06 실기 로그:
         PermissionsInspector "neverForLocation ... will block detection of bluetooth beacons").
         플래그가 없으므로 스캔은 위치 권한(FINE_LOCATION) 승인과 함께 동작하며,
         좌표는 여전히 어디서도 읽지 않는다(PI-3 회귀 테스트 보증). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!-- 키오스크 비컨 광고 (KO-3) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:label="출석핑"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
